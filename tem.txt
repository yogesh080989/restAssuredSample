index=DPM source="/logs/flink/prod/dpm-alerts-processor/*/alerts-processor-app.log"
"Message published"
| spath
| spath path=notice.noticeType output=noticeType
| where noticeType=6350
| spath path=notice.noticePropertyList{} output=prop
| mvexpand prop
| spath input=prop path=name output=fieldName
| spath input=prop path=value output=fieldValue
| where fieldName="confirmationNumber"
| eval confirmationNumber=fieldValue
| eval AP_CREATED_TIME=strftime(_time, "%Y-%m-%d %H:%M:%S")
| table confirmationNumber AP_CREATED_TIME
